// Fonts are loaded via Google Fonts in pages/_document.tsx
// Epilogue (headings) and Plus Jakarta Sans (body) are the only fonts in use.
// CSS variables --font-heading and --font-body are set in styles/globals.css.
